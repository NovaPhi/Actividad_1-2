# Activity 1.2 - Regular languages

Reference for math symbols:
https://www.overleaf.com/learn/latex/List_of_Greek_letters_and_math_symbols

## Names:
- Luis Jaime Arias
- Pablo Sedano Morlett


## Exercises:

1. Give a recursive definition of the set of strings over $\Sigma = \{a, b\}$ that have at least one $a$, and where any $b$ must be followed by an $a$.
For example: $a$, $aaaaa$ and $aabaaaba$ belong to the set, but $bab$, $ababba$ no not belong in the set

    _**SOLUTION:**_

I. Basis: $a \in L$ 
II. Recursive Step: If $s \in L$ then $sa \in L$ and $sba \in L$
III. Closure: A string $s \in L$ only if it can be obtained from the basis using a finite number of applications of the recursive step.

2. Let $X = \{aa, cb\}$ and $Y = \{\lambda, b, ac\}$.

    a. List the strings in set $XY$

    #|string|#|string|#|string
    -|------|-|------|-|------
    1|   aab   |2|  aaac   |3|    cbb


    b. How many strings of length 6 are there in $X^*$?
    8

    c. List the strings in set $Y^*$ of length three or less

    #|string|#|string|#|string
    -|------|-|------|-|------
    1|   $\lambda$   |2|    b  |3|   ac 
    

    d. List the strings in set $X^* Y^*$ of length four or less

    #|string|#|string|#|string
    -|------|-|------|-|------
    1| aab     |2| aaac     |3|  aa  
    4| cbb     |5|   cbac   |6|cb


3. Give a recursive definition of the set $S = \{ b^ia^j | 0 ≤ i ≤ j ≤ 2i\}$.  
The values of $i$ and $j$ represent how many times a symbol is repeated.
For example: $ba$, $bbaaa$ and $bbaaaa$ belong to this set.

    _**SOLUTION:**_

    **I. Basis:** $\lambda \in S$

    **II. Recursive Step:** if $w \in S$ then $bwa \in S$ , $bwaa \in S$

    **III. Closure:** A string $w \in S$ only if it can be obtained from the
    basis using a finite number of applications of the recursive step.


4. Let $L$ be the set of strings over $\{a, b\}$ generated by the recursive
   definition

    **I. Basis:** $b \in L$

    **II. Recursive Step**: If $u \in L$ then $ub \in L$, $uab \in L$, $uba \in
    L$ and $bua \in L$

    **III. Closure**: A string $u \in L$ only if it can be obtained from the
    basis using a finite number of applications of the recursive step.

    Answer the following questions:

    **a.** List the elements in the sets $L_0, L_1, L_2$
        
    $L_0 = {b} , L_1 = {bb,bab,bba},$
    
    $L_2 = {bbb,bbab,bbba,babb,babab,babba,bbaba,bbaab,bbbaa}$


    **b.** Is the string $bbaaba$ in $L$? If so, trace how it is produced.
    If not, explain why not.
    $bbaaba \notin L$ because the number of a's is equal to the number of b's which is not possible as each recursive step  $+1b$ and $+0a or +1a$ therefore after $n$ steps $b = 1+n$ and $a \leq n$

    **c.** Is the string $bbbaabb$ in $L$? If so, trace how it is produced.
    If not, explain why not
    As concluded in answer b) after $n$ recursive steps the number of $b= n+1$ and $a \leq n$ therefore the string presented with a $b$ count of $5$ and an $a$ count of $2$ it satisfies the rule $a \leq b-1$ so the string $w \in L$

    to build it 
    
    *1 start b:* $b $
        
    *2 apply ub:* $bb$

    *3 apply ub:* $bbb$

    *4 apply uba:* $bbba$
    
    *5 apply uab:* $bbbaab$

    *6 apply ub:* $bbbaabb$
    
5. Prove, using induction on the length of a string, that $(w^R)^R = w$ for all
   string $w \in \Sigma$

    _**SOLUTION:**_

    **I. Basis:** $\lambda ^R = \lambda$ therefore $ (\lambda^R)^R = \lambda = w$

    **II. Inductive Hypothesis:** all strings x of length n $(x^R)^R = x$ 

    **III. Inductive Step:** let w string be of length $n+1$ 
    
    $((w^R)^R = w+1)$ $ = (w=xa)$
    
    where $x$ string lenght $n$ , $a \in \Sigma$

    reverese

    $w^R = (xa)^R = ax^R$

    reverse again
    
    $(w^R)^R = (ax^R)^R$

    use the definition of reverse

    $(ax^R)^R = (x^R)^R a$

    induction hypothesis
    
    $(x^R)^R = x$

    substitute

    $(w^R)^R = xa$

    therefore

    $(w^R)^R = xa = w$